
<view  class="info">
    <view>省 份</view>
    <picker bindchange="bindPickerChange" value="{{provinceId}}" range="{{province}}">
        <view class="picker">
            {{province[provinceId]}}
        </view>
    </picker>
</view> 

<view class="info">
    <view>城 市</view>
    <picker bindchange="bindPickerCity" value="{{cityId}}" range="{{cityArea[provinceId].city[cityId].name}}">
        <view class="picker">
            {{city[provinceId].city[0].name}}
        </view>
    </picker>
</view>